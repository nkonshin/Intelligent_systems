# Экспертная система диагностики простудных заболеваний
## Прототип ядра ЭС - Практическая работа №3

Работающий прототип экспертной системы на основе библиотеки **experta** (Python).

---

## Описание

Экспертная система для автоматизированной диагностики распространенных простудных заболеваний на основе введенных симптомов.

**Реализованные возможности:**
- 7 продукционных правил диагностики
- Механизм вывода с цепочками рассуждений
- Блок объяснений (трассировка сработавших правил)
- Вычисление факторов уверенности (CF)
- Интерактивный ввод через консоль
- Загрузка тестовых сценариев из JSON
- Генерация рекомендаций с приоритетами

---

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

Требуется: Python 3.7+

**Важно для Python 3.10+:**  
Код содержит автоматический патч совместимости для работы с `experta` на современных версиях Python. Патч применяется автоматически при импорте.

### 2. Запуск программы

```bash
python medical_expert_system.py
```

### 3. Выбор режима

После запуска появится меню:

```
Выберите режим работы:
1. Интерактивный ввод симптомов
2. Тестовый сценарий #1 (Грипп у пожилого пациента)
3. Тестовый сценарий #2 (COVID-19)
4. Тестовый сценарий #3 (Критическая ситуация)
0. Выход
```

---

## Режимы работы

### Режим 1: Интерактивный ввод

Система задаст серию вопросов о:
- Возрасте пациента
- Наличии хронических заболеваний
- Температуре тела
- Характере начала (резкое/постепенное)
- Респираторных симптомах (насморк, кашель, боль в горле)
- Общих симптомах (слабость, ломота в мышцах)
- Специфических симптомах (потеря обоняния/вкуса)
- Критических симптомах (тяжелая одышка, низкая сатурация)

**Пример диалога:**
```
Возраст пациента: 35
Есть ли хронические заболевания? (да/нет): нет
Температура тела (°C): 38.1
Характер начала (резкое/постепенное): постепенное
Есть насморк? (да/нет): да
...
```

### Режим 2-4: Тестовые сценарии

Автоматически загружают предустановленные данные из JSON файлов.

**Сценарий #1:** Грипп у пожилого пациента  
**Сценарий #2:** Подозрение на COVID-19  
**Сценарий #3:** Критическое состояние (пневмония с дыхательной недостаточностью)

---

## Реализованные правила

| ID | Название | CF | Описание |
|---|---|---|---|
| **R001** | Грипп типа A | 0.85 | Высокая температура + резкое начало + ломота + слабость |
| **R002** | ОРВИ | 0.90 | Умеренная температура + насморк + боль в горле |
| **R003** | COVID-19 | 0.75 | Потеря обоняния/вкуса + температура + сухой кашель |
| **R006** | Легкая простуда | 0.95 | Нормальная температура + легкий насморк |
| **R005** | Пневмония | 0.70 | Длительное заболевание + кашель с мокротой + боль в груди |
| **R012** | Критическое состояние | 1.0 | Тяжелая одышка / низкая сатурация O₂ |
| **R008** | Группа риска (пожилые) | 0.88 | Возраст >65 + грипп/ОРВИ + хронические болезни |

---

## Пример вывода

```
======================================================================
                    РЕЗУЛЬТАТЫ ДИАГНОСТИКИ
======================================================================

УСТАНОВЛЕННЫЕ ДИАГНОЗЫ:

  • Грипп типа A
    Уверенность: 85% | Тяжесть: средняя

УРОВЕНЬ РИСКА:
  • ВЫСОКИЙ (88%)
    Причина: пожилой_возраст_с_хроническими_заболеваниями

РЕКОМЕНДАЦИИ:

  [ЛЕЧЕНИЕ] (приоритет: высокий)
    Постельный режим, противовирусные препараты (осельтамивир), 
    обильное питье

  [МОНИТОРИНГ] (приоритет: высокий)
    Обязательная консультация врача, ежедневный мониторинг, 
    противовирусная терапия

======================================================================
                    ОБЪЯСНЕНИЕ РАССУЖДЕНИЙ
======================================================================

Сработало правил: 2

1. [R001] Диагностика гриппа типа A
   Время: 14:23:15
   Обоснование: Высокая температура (>38.5°C) + резкое начало + 
   сильная ломота в мышцах + выраженная слабость указывают на грипп типа A

2. [R008] Группа риска: пожилые
   Время: 14:23:15
   Обоснование: Возраст >65 лет + грипп/ОРВИ + хронические заболевания = 
   высокий риск осложнений

======================================================================
```

---

## Архитектура

### Классы фактов

```python
class Patient(Fact):        # Данные пациента
class Symptom(Fact):        # Симптомы
class Diagnosis(Fact):      # Диагноз
class RiskLevel(Fact):      # Уровень риска
class Recommendation(Fact): # Рекомендации
```

### Движок вывода

```python
class MedicalDiagnosisEngine(KnowledgeEngine):
    # Прямой вывод (forward chaining)
    # Автоматическая активация правил при добавлении фактов
```

### Правила

Определяются через декоратор `@Rule`:

```python
@Rule(
    Symptom(name='температура', value=P(lambda x: x > 38.5)),
    Symptom(name='начало', value='резкое'),
    # ... другие условия
)
def rule_001_flu_type_a(self):
    # Действия при срабатывании
    self.declare(Diagnosis(...))
    self.declare(Recommendation(...))
```

---

## Тестовые сценарии

### Сценарий #1: scenario_1_elderly_flu.json

**Описание:** Грипп у пожилого пациента (72 года) с хроническими заболеваниями

**Ожидаемый результат:**
- Диагноз: Грипп типа A (CF: 0.85)
- Уровень риска: Высокий (CF: 0.88)
- Правила: R001 → R008

**Цепочка вывода:**
```
Симптомы → [R001] Грипп → [R008] Высокий риск
```

---

### Сценарий #2: scenario_2_covid.json

**Описание:** Молодой пациент (35 лет) с потерей обоняния и вкуса

**Ожидаемый результат:**
- Диагноз: Подозрение на COVID-19 (CF: 0.75)
- Рекомендация: Срочный ПЦР-тест
- Правила: R003

**Цепочка вывода:**
```
Потеря обоняния/вкуса + температура → [R003] COVID-19
```

---

### Сценарий #3: scenario_3_critical.json

**Описание:** Пациент с пневмонией и дыхательной недостаточностью

**Ожидаемый результат:**
- Диагнозы: Риск пневмонии (CF: 0.70) + Критическое состояние (CF: 1.0)
- Рекомендация: Немедленная госпитализация
- Правила: R005, R012

**Цепочка вывода:**
```
Длительное заболевание + тяжелые симптомы → [R005] Пневмония
Тяжелая одышка + низкая сатурация → [R012] Критическое состояние
```

---

## Структура проекта

```
medical_expert_system/
│
├── medical_expert_system.py      # Основной код ЭС
├── scenario_1_elderly_flu.json   # Тестовый сценарий #1
├── scenario_2_covid.json         # Тестовый сценарий #2
├── scenario_3_critical.json      # Тестовый сценарий #3
├── requirements.txt              # Зависимости Python
├── README.md                     # Эта инструкция
├── report_lab3.md               # Полный отчет о работе
├── defense_guide_lab3.md        # Шпаргалка для защиты
└── ИСПРАВЛЕНИЯ.md               # Список исправлений и улучшений
```

---

## Исправления и улучшения

### Проблема 1: Совместимость с Python 3.10+
**Решение:** Добавлен автоматический патч для `collections.Mapping` → `collections.abc.Mapping`

### Проблема 2: Ошибки типов в предикатах
**Решение:** Добавлена проверка типов `isinstance(x, (int, float))` во все числовые сравнения

### Проблема 3: Изменение оригинальных данных
**Решение:** Создание копии словаря в `load_scenario` перед модификацией

Подробнее см. файл `ИСПРАВЛЕНИЯ.md`

---

## Объяснение работы

### Как работает механизм вывода?

1. **Декларация фактов:**
```python
engine.declare(Patient(age=35))
engine.declare(Symptom(name='температура', value=39.2))
```

2. **Автоматическая активация правил:**
- Движок проверяет условия всех правил
- Если условия правила выполнены → правило срабатывает
- Правило добавляет новые факты (диагноз, рекомендации)

3. **Цепочки вывода:**
- Новые факты могут активировать другие правила
- Например: Грипп (R001) → Высокий риск у пожилых (R008)

4. **Объяснения:**
- Каждое правило записывает свое срабатывание
- Результат: полная трассировка рассуждений

---

## Особенности реализации

### Использование предикатов

```python
# Проверка диапазона
Symptom(name='температура', value=P(lambda x: x > 38.5))

# Проверка любого значения
Symptom(name='одышка', value=MATCH.any_value)
```

### Логические операторы

```python
# ИЛИ
OR(
    Symptom(name='потеря_обоняния', value=True),
    Symptom(name='потеря_вкуса', value=True)
)

# И
AND(
    Symptom(name='температура', value=P(lambda x: x > 40)),
    Symptom(name='жаропонижающие_помогают', value=False)
)

# НЕ
NOT(Diagnosis())
```

### Факторы уверенности

Каждый диагноз имеет фактор уверенности (CF):
- **1.0 (100%)** - абсолютная уверенность (критические состояния)
- **0.85-0.95** - высокая уверенность (специфические симптомы)
- **0.70-0.80** - умеренная уверенность (требуется подтверждение)

---

## Исправления в процессе разработки

### Исправление №1: Сосуществование диагнозов

**Проблема:** Правило R005 (Пневмония) не срабатывало вместе с R012 (Критическое состояние)

**Причина:** Условие `NOT(Diagnosis())` блокировало R005 после срабатывания R012

**Решение:** Удалено `NOT(Diagnosis())` из R005, так как пневмония может быть причиной критического состояния

### Исправление №2: Сортировка рекомендаций

**Проблема:** Рекомендации выводились в случайном порядке

**Решение:** Добавлена сортировка по приоритету (экстренный → критический → высокий → средний → низкий)

---


## Часто задаваемые вопросы (FAQ)

### Зачем нужна библиотека experta?

**experta** - это готовый движок для экспертных систем на Python.

**Что делает за нас:**

1. **Машина вывода** - автоматически проверяет все правила и решает, какие срабатывают
2. **Рабочая память** - хранит факты и отслеживает изменения
3. **Паттерн-матчинг** - сопоставляет факты с условиями правил (IF часть)
4. **Разрешение конфликтов** - если несколько правил подходят, выбирает порядок срабатывания

**Без experta пришлось бы писать вручную:**
```python
# Проверять каждое правило
if temp > 38.5 and onset == 'резкое' and muscle_pain == 'высокая':
    diagnosis = 'Грипп'
if temp > 37.5 and runny_nose and sore_throat:
    diagnosis = 'ОРВИ'
# ... и так для всех правил
```

**С experta:**
```python
@Rule(Symptom(name='температура', value=P(lambda x: x > 38.5)))
def rule_001_flu_type_a(self):
    self.declare(Diagnosis(name='Грипп типа A'))

engine.run()  # Всё проверяется автоматически
```

**Короче:** experta = готовый фреймворк для продукционных правил, чтобы не писать логику экспертной системы с нуля.

---

### Зачем нужен декоратор @Rule?

Декоратор `@Rule` - это **механизм работы библиотеки experta**, а не просто украшение кода.

**Зачем нужен:**

1. **Регистрация правила** - помечает метод как продукционное правило, которое движок должен проверять
2. **Автоматическое сопоставление** - движок сам проверяет условия в `@Rule(...)` и вызывает метод, если они выполнены
3. **Паттерн-матчинг** - experta анализирует факты в рабочей памяти и сравнивает с условиями в декораторе

**Без декоратора:**
```python
def rule_001_flu_type_a(self):
    # Движок не знает, что это правило и когда его вызывать
    # Метод просто висит мертвым грузом
```

**С декоратором:**
```python
@Rule(
    Symptom(name='температура', value=P(lambda x: x > 38.5)),
    Symptom(name='начало', value='резкое')
)
def rule_001_flu_type_a(self):
    # Движок автоматически вызовет метод, 
    # когда в памяти появятся подходящие факты
    self.declare(Diagnosis(name='Грипп типа A'))
```

**Аналогия:** Это как подписка на события - декоратор говорит "вызови меня, когда увидишь эти факты", а движок следит за этим автоматически.

**Как это работает:**

1. При создании класса `MedicalDiagnosisEngine` experta сканирует все методы с декоратором `@Rule`
2. Регистрирует их в своей внутренней базе правил
3. При вызове `engine.run()` движок:
   - Проверяет все зарегистрированные правила
   - Сопоставляет условия с фактами в памяти
   - Автоматически вызывает методы правил, условия которых выполнены
   - Повторяет процесс, пока не перестанут срабатывать новые правила

**Без experta и декораторов** пришлось бы писать всю эту логику вручную - проверку условий, управление памятью, разрешение конфликтов между правилами и т.д.

---

## Контакты и поддержка

При возникновении вопросов или проблем:
1. Проверьте раздел FAQ выше
2. Изучите файл `defense_guide_lab3.md` - там есть ответы на типичные вопросы защиты
3. Посмотрите полный отчет в `report_lab3.md`
